# Tasks: Add Note Subcategory Filter

## Overview

本文档列出实现笔记子分类过滤功能的具体任务，按执行顺序排列。

## Task Breakdown

### 1. 创建子分类提取工具函数

**任务**: 在 `src/utils/helper.ts` 中添加子分类提取函数

**验收标准**:

- [x] 创建 `extractSubcategories(categoryName: string, directoryData: DirectoryNode[]): string[]` 函数
- [x] 从 `note-directory-structure.json` 数据中提取指定大分类的子目录名称
- [x] 返回按字母序排序的子分类名称数组
- [x] 处理无子分类的情况（返回空数组）
- [x] 添加 TypeScript 类型定义

**依赖**: 无

**预估工作量**: 30分钟

---

### 2. 为子分类提取函数添加单元测试

**任务**: 在 `src/utils/helper.test.ts` 中添加测试

**验收标准**:

- [x] 测试正常提取子分类的情况
- [x] 测试无子分类时返回空数组
- [x] 测试不存在的大分类
- [x] 测试空数据或格式错误的数据
- [x] 测试返回结果的排序正确性
- [x] 所有测试通过

**依赖**: 任务 1

**预估工作量**: 30分钟

---

### 3. 在 NoteList 组件中添加子分类过滤状态

**任务**: 修改 `src/components/NoteList/NoteList.tsx`

**验收标准**:

- [x] 添加 `selectedSubcategory` 状态（类型: `string | null`）
- [x] 使用 `useMemo` 从全局数据中提取当前大分类的子分类列表
- [x] 导入 `note-directory-structure.json` 数据
- [x] 确保大分类切换时重置子分类过滤状态

**依赖**: 任务 1

**预估工作量**: 20分钟

---

### 4. 实现子分类过滤逻辑

**任务**: 修改 `filteredAndSortedData` 计算逻辑

**验收标准**:

- [x] 在现有过滤逻辑中添加子分类过滤步骤
- [x] 过滤优先级：全局搜索 -> 子分类过滤 -> 排序
- [x] 正确处理笔记路径（`category/subcategory/filename.md`）
- [x] 正确处理根目录文件（`category/filename.md`）
- [x] 选中“全部”（`selectedSubcategory === null`）时显示所有笔记
- [x] 过滤后重置分页到第1页

**依赖**: 任务 3

**预估工作量**: 30分钟

---

### 5. 创建 CategoryFilter 子组件（或内联实现）

**任务**: 在 NoteList 组件中添加子分类标签栏

**验收标准**:

- [x] 使用 Ant Design 的 `Tag.CheckableTag` 组件
- [x] 渲染“全部”标签 + 所有子分类标签
- [x] “全部”标签默认选中
- [x] 点击标签时更新 `selectedSubcategory` 状态
- [x] 支持点击已选中标签取消选择（回到“全部”）
- [x] 无子分类时不显示过滤栏
- [x] 标签支持自动换行

**依赖**: 任务 3

**预估工作量**: 45分钟

---

### 6. 添加 CategoryFilter 样式

**任务**: 在 `src/components/NoteList/style.scss` 中添加样式

**验收标准**:

- [x] 添加过滤栏容器样式（边距、padding）
- [x] 设置标签间距（水平 8px，垂直 4px）
- [x] 确保标签自动换行
- [x] 添加响应式断点适配（移动设备优化）
- [x] 选中和未选中状态的视觉区分
- [x] 鼠标悬停效果
- [x] 与现有设计风格保持一致

**依赖**: 任务 5

**预估工作量**: 30分钟

---

### 7. 更新 NoteList 组件测试

**任务**: 在 `src/components/NoteList/NoteList.test.tsx` 中添加测试

**验收标准**:

- [x] 测试有子分类时显示过滤栏
- [x] 测试无子分类时不显示过滤栏
- [x] 测试点击子分类标签进行过滤
- [x] 测试点击“全部”标签取消过滤
- [x] 测试子分类过滤与全局搜索结合
- [x] 测试切换大分类时重置过滤状态
- [x] 测试子分类过滤与排序结合
- [x] 测试分页重置
- [x] 所有现有测试仍然通过

**依赖**: 任务 4, 5, 6

**预估工作量**: 60分钟

---

### 8. 添加国际化支持

**任务**: 在 `src/i18n/locales/` 中添加相关翻译

**验收标准**:

- [x] 在中文语言文件中添加“全部”翻译
- [x] 在英文语言文件中添加 "All" 翻译
- [x] 在日文语言文件中添加 "すべて" 翻译
- [x] 在 NoteList 组件中使用 `t()` 函数
- [x] 验证多语言切换正常工作
- [ ] 在英文语言文件中添加 "All" 翻译
- [ ] 在日文语言文件中添加 "すべて" 翻译
- [ ] 在 NoteList 组件中使用 `t()` 函数
- [ ] 验证多语言切换正常工作

**依赖**: 任务 5

**预估工作量**: 15分钟

---

### 9. 手动测试和边界情况验证

**任务**: 在浏览器中进行全面测试

**验收标准**:

- [ ] 测试所有大分类（有子分类和无子分类）
- [ ] 测试大量子分类时的渲染性能和布局
- [ ] 测试单个子分类的情况
- [ ] 测试快速切换大分类和子分类
- [ ] 测试与全局搜索、排序、分页的组合
- [ ] 测试不同屏幕尺寸的响应式布局
- [ ] 测试主题切换（明暗主题）下的视觉效果
- [ ] 测试多语言切换
- [ ] 验证所有交互的流畅性和视觉反馈

**依赖**: 任务 1-8

**预估工作量**: 45分钟

---

### 10. 代码审查和优化

**任务**: 检查代码质量和性能

**验收标准**:

- [x] 运行 ESLint，确保无警告和错误
- [x] 检查代码注释的完整性
- [x] 验证 TypeScript 类型安全
- [x] 优化 `useMemo` 和 `useEffect` 的依赖项
- [x] 检查是否有不必要的重渲染
- [x] 确保遵循项目代码规范（命名、格式等）
- [x] 提交前运行完整测试套件

**依赖**: 任务 1-9

**预估工作量**: 30分钟

---

### 11. 文档更新（可选）

**任务**: 更新相关文档

**验收标准**:

- [ ] 在 README 或用户文档中说明子分类过滤功能
- [ ] 更新组件文档（如果有）
- [ ] 添加功能截图或 GIF 演示（可选）

**依赖**: 任务 10

**预估工作量**: 20分钟

---

## Task Summary

- **总任务数**: 11
- **预估总工作量**: 约 6 小时
- **关键路径**: 任务 1 -> 3 -> 4 -> 5 -> 6 -> 7 -> 9 -> 10
- **可并行任务**: 任务 2（测试）可在任务 3-5 进行时同步进行；任务 8（国际化）可在任务 7 之前完成

## Validation Checklist

完成所有任务后，确保：

- [ ] 所有单元测试通过 (`npm run test`)
- [ ] 无 ESLint 错误或警告 (`npm run lint`)
- [ ] 无 TypeScript 类型错误 (`npm run build`)
- [ ] 手动测试所有功能场景
- [ ] 代码符合项目规范
- [ ] 提交信息清晰描述变更

## Rollout Strategy

1. **第一阶段**: 完成任务 1-4（核心功能）
2. **第二阶段**: 完成任务 5-6（UI 实现）
3. **第三阶段**: 完成任务 7-8（测试和国际化）
4. **第四阶段**: 完成任务 9-11（验证和文档）

每个阶段完成后进行验收，确保功能正常再进入下一阶段。

## Risk Mitigation

- **性能风险**: 如果子分类数量超过 20 个，考虑添加虚拟滚动或分页
- **兼容性风险**: 每个任务完成后运行现有测试，确保无回归
- **用户体验风险**: 在任务 9 中充分测试各种边界情况
